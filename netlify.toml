[build]
  command = "echo 'No build needed for serverless functions'"
  publish = "."

[functions]
  directory = "api"
  python_version = "3.11"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[build.environment]
  PYTHON_VERSION = "3.11"
